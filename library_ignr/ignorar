
#" PIP "

# 'pip install -r (nome do pacote), instala todos os pacotes dentro de um arquivo.
#


#" . VENV "
# para entrar no .venv vc deve jogar o seguinte comando no terminal (.\.venv\Scripts\activate)
# para sair do .venv vc deve digitar no terminal (deactivate)


#  " GIT "
# lista de comandos
# git status, git add (file), git commit -m "qualquer coisa", git push, git push origin main, git ls.
